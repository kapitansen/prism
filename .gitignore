# Dependencies
node_modules/

# Build output
dist/
build/
*.tsbuildinfo
# Stray compiled output next to TS sources (e.g. an editor compiling seed.ts)
apps/api/prisma/*.js
apps/api/prisma/*.js.map
apps/api/prisma/*.d.ts

# Logs
*.log

# Environment variables (secrets never go in git)
.env
.env.*
!.env.example

# OS files
.DS_Store

# Personal data and import script
apps/api/prisma/import.ts
personal/
backups/

# Personal, non-committed notes (mentoring rules etc.) and any *.local.md twin
# (e.g. skills/*.local.md — local working translations of the AI-settings files).
CLAUDE.local.md
*.local.md

# Russian planning spec — kept local; the public showcase is English docs/README
spec.md

# Internal planning / agent guidance — kept local for now; re-add when polished
CLAUDE.md
ROADMAP.md
